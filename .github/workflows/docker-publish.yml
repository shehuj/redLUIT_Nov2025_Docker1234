name: Build, Test & Publish Docker Image

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened
      - closed

  push:
    branches:
      - dev
      - main
    tags:
      - 'v*.*.*'

env:
  REGISTRY: docker.io
  IMAGE_NAME: ${{ secrets.DOCKER_USERNAME }}/${{ secrets.IMAGE_NAME }}  # ✅ Full image path
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}

jobs:
  test-pr:
    name: Test on Pull Request
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.event.action != 'closed'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          python -m pip install flake8 pytest ruff
      
      - name: Run tests
        run: |
          echo "Running test suite..."
          pytest tests/ -v
          ruff check .

  build-publish:
    name: Build, Scan & Publish Docker Image
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'push' ||
      (github.event_name == 'pull_request' && github.event.action == 'closed' && github.event.pull_request.merged == true)

    permissions:
      security-events: write
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ═══════════════════════════════════════════════════════
      # CODE SECURITY SCAN - Semgrep
      # ═══════════════════════════════════════════════════════
      - name: Run Semgrep code scan
        uses: docker://returntocorp/semgrep:latest
        continue-on-error: true
        with:
          entrypoint: /bin/sh
          args: -c "semgrep ci --suppress-errors --sarif --config auto > semgrep-results.sarif || true"
        env:
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}

      - name: Upload Semgrep results to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: semgrep-results.sarif
          category: 'semgrep-code-scan'

      # ═══════════════════════════════════════════════════════
      # DOCKER BUILD SETUP
      # ═══════════════════════════════════════════════════════
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # ✅ Verify credentials before login
      - name: Verify Docker Hub credentials
        run: |
          echo "Checking Docker Hub credentials..."
          echo ${{ env.DOCKER_PASSWORD }} | docker login -u ${{ env.DOCKER_USERNAME }} --password-stdin
          if [ -z "${{ secrets.DOCKER_USERNAME }}" ]; then
            echo "❌ DOCKER_USERNAME secret is not set"
            exit 1
          fi
          if [ -z "${{ secrets.DOCKER_PASSWORD }}" ]; then
            echo "❌ DOCKER_PASSWORD secret is not set"
            exit 1
          fi
          if [ -z "${{ secrets.IMAGE_NAME }}" ]; then
            echo "❌ IMAGE_NAME secret is not set"
            exit 1
          fi
          echo "✅ All required secrets are set"
          echo "📦 Full image path will be: ${{ env.IMAGE_NAME }}"

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # ✅ Verify login was successful
      - name: Verify Docker Hub login
        run: |
          echo "Testing Docker Hub authentication..."
          docker info | grep Username || echo "⚠️ Username not shown in docker info"
          echo "✅ Successfully logged in to Docker Hub"

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      # ✅ Show what tags will be created
      - name: Show image tags
        run: |
          echo "The following tags will be created:"
          echo "${{ steps.meta.outputs.tags }}"

      # ═══════════════════════════════════════════════════════
      # BUILD IMAGE & LOAD FOR SCANNING
      # ═══════════════════════════════════════════════════════
      - name: Build Docker image and load locally
        id: build-image
        uses: docker/build-push-action@v6
        with:
          context: ./docker01
          file: ./docker01/Dockerfile
          push: false
          load: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ═══════════════════════════════════════════════════════
      # IMAGE SECURITY SCAN - Trivy
      # ═══════════════════════════════════════════════════════
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        continue-on-error: true
        with:
          scan-type: 'image'
          image-ref: ${{ fromJSON(steps.meta.outputs.json).tags[0] }}
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH,MEDIUM'
          exit-code: '0'

      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: trivy-results.sarif
          category: 'trivy-image-scan'

      - name: Run Trivy vulnerability scanner (Table Output)
        uses: aquasecurity/trivy-action@master
        continue-on-error: true
        with:
          scan-type: 'image'
          image-ref: ${{ fromJSON(steps.meta.outputs.json).tags[0] }}
          format: 'table'
          severity: 'CRITICAL,HIGH'

      # ═══════════════════════════════════════════════════════
      # PUBLISH IMAGE TO DOCKER HUB
      # ═══════════════════════════════════════════════════════
      - name: Build and Push Docker image to Docker Hub
        id: build-and-push
        uses: docker/build-push-action@v6
        with:
          context: ./docker01
          file: ./docker01/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64  # ✅ Single platform for faster builds
          
      # ═══════════════════════════════════════════════════════
      # BUILD SUMMARY
      # ═══════════════════════════════════════════════════════
      - name: Generate build summary
        run: |
          echo "## 🚀 Docker Image Published Successfully!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Image Details" >> $GITHUB_STEP_SUMMARY
          echo "- **Registry:** Docker Hub" >> $GITHUB_STEP_SUMMARY
          echo "- **Repository:** ${{ env.IMAGE_NAME }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Digest:** \`${{ steps.build-and-push.outputs.digest }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🏷️ Tags Published" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔗 Pull Command" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "docker pull ${{ fromJSON(steps.meta.outputs.json).tags[0] }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📊 Security Scans" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Semgrep code scan completed" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Trivy image scan completed" >> $GITHUB_STEP_SUMMARY
          echo "- 📋 Check the [Security tab](/${{ github.repository }}/security/code-scanning) for detailed results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🤝 Thanks for using our CI/CD pipeline!" >> $GITHUB_STEP_SUMMARY
          echo "We appreciate your support and contributions." >> $GITHUB_STEP_SUMMARY
